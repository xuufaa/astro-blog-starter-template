---
const today = new Date();
---

<footer>
  <div class="footer-container">
    <p class="footer-title">&copy; <span id="year"></span> 刘旭发（Lau Xufa）</p>
    <p class="footer-subtitle">All rights reserved.</p>

    <div class="social-links">
      <a href="https://m.webtoo.ls/@astro" target="_blank" aria-label="Follow Astro on Mastodon">
        <!-- Mastodon SVG -->
        <svg viewBox="0 0 16 16" width="28" height="28" fill="currentColor" aria-hidden="true">
          <path d="..."></path>
        </svg>
      </a>
      <a href="https://twitter.com/astrodotbuild" target="_blank" aria-label="Follow Astro on Twitter">
        <!-- Twitter SVG -->
        <svg viewBox="0 0 16 16" width="28" height="28" fill="currentColor" aria-hidden="true">
          <path d="..."></path>
        </svg>
      </a>
      <a href="https://github.com/withastro/astro" target="_blank" aria-label="Go to Astro GitHub">
        <!-- GitHub SVG -->
        <svg viewBox="0 0 16 16" width="28" height="28" fill="currentColor" aria-hidden="true">
          <path d="..."></path>
        </svg>
      </a>
    </div>
  </div>
</footer>

<script>
  document.getElementById("year").textContent = new Date().getFullYear();
</script>

<style>
  footer {
    background: linear-gradient(var(--gray-gradient));
    color: rgb(var(--gray));
    text-align: center;
    padding: 3em 1em 6em 1em;
    font-family: 'Helvetica Neue', sans-serif;
  }

  .footer-container {
    max-width: 800px;
    margin: 0 auto;
  }

  .footer-title {
    font-size: 1.2rem;
    font-weight: bold;
    margin-bottom: 0.2em;
  }

  .footer-subtitle {
    font-size: 0.95rem;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 1.5em;
  }

  .social-links {
    display: flex;
    justify-content: center;
    gap: 1.2em;
  }

  .social-links a {
    color: rgb(var(--gray));
    transition: color 0.3s ease;
  }

  .social-links a:hover {
    color: rgb(var(--gray-dark));
  }

  svg {
    vertical-align: middle;
  }
</style>
