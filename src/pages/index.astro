---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const SITE_TITLE = "刘旭发的主页";
const SITE_DESCRIPTION = "技术与法律笔记，生活与成长分享";
---

<!DOCTYPE html>
<html lang="zh">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      html {
        font-size: clamp(14px, 1.5vw, 18px);
        word-break: break-word;
      }

      body {
        font-family: "Noto Sans SC", "Helvetica Neue", sans-serif;
        margin: 0;
        padding: 0;
        line-height: 1.6;
        color: #333;
        background-color: #fff;
      }

      main {
        padding: 2rem;
        max-width: 800px;
        margin: 0 auto;
      }

      h1 {
        font-size: clamp(28px, 4vw, 36px);
        font-weight: 700;
        margin-bottom: 1rem;
        color: #111;
      }

      p {
        font-size: clamp(16px, 1.6vw, 20px);
        margin-bottom: 1rem;
      }

      footer {
        text-align: center;
        font-size: clamp(12px, 1.2vw, 16px);
        color: #666;
        padding: 2rem 1rem;
        background-color: #f9f9f9;
      }
<style>
  html {
    font-size: clamp(14px, 1.5vw, 18px);
    word-break: break-word;
  }

  body {
    font-family: "Noto Sans SC", "Helvetica Neue", sans-serif;
    margin: 0;
    padding: 0;
    line-height: 1.6;
    color: #333;
    background-color: #fff;
  }

  main {
    padding: 2rem;
    max-width: 800px;
    margin: 0 auto;
  }

  h1 {
    font-size: clamp(28px, 4vw, 36px);
    font-weight: 700;
    margin-bottom: 1rem;
    color: #111;
  }

  p {
    font-size: clamp(16px, 1.6vw, 20px);
    margin-bottom: 1rem;
  }

  footer {
    text-align: center;
    font-size: clamp(12px, 1.2vw, 16px);
    color: #666;
    padding: 2rem 1rem;
    background-color: #f9f9f9;
  }

  /* ✅ 新增的视频样式 */
  .video-section {
    margin-top: 3rem;
    padding: 1.5rem;
    background-color: #f0f4f8;
    border-radius: 12px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
    text-align: center;
  }

  .video-section h2 {
    font-size: clamp(20px, 2vw, 28px);
    margin-bottom: 1rem;
    color: #222;
  }

  .video-section video {
    max-width: 100%;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  }

  .video-section .caption {
    font-size: 0.95rem;
    color: #555;
    margin-top: 0.75rem;
  }
</style>
</style>
  </head>
  <body>
    <Header />
 
<main>
  <h1>欢迎来到我的主页</h1>
  <p>你好，我是刘旭发。这是我的博客，记录一些技术探索、法律笔记与生活点滴。</p>
  <p>内容持续更新中，欢迎常来看看 :)</p>

  <section class="video-section">
    <h2>🎵 1985年版《明天会更好》</h2>
    <video controls poster="https://r2xu.567998.xyz/video/Tomorrow_Will_Be_Better_Cover.jpg">
      <source src="https://r2xu.567998.xyz/video/Tomorrow-will-be-better.MP4" type="video/mp4" />
      您的浏览器不支持视频播放。
    </video>
    <p class="caption">这首由罗大佑等人联合制作的公益歌曲，激励了一代人。</p>
  </section>
</main>













    </main>
    <Footer />
  </body>
</html>
